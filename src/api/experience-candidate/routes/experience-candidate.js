'use strict';

/**
 * experience-candidate router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::experience-candidate.experience-candidate');
