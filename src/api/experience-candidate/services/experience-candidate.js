'use strict';

/**
 * experience-candidate service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::experience-candidate.experience-candidate');
